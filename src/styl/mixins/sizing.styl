width-scope(value, unit = '')
  {props-sizing-width-property}: getWidthValue(value, unit) !important

height-scope(value, unit = '')
  {props-sizing-height-property}: getHeightValue(value, unit) !important

min-width-scope(value, unit = '')
  {props-sizing-min-width-property}: getWidthValue(value, unit) !important

min-height-scope(value, unit = '')
  {props-sizing-min-height-property}: getHeightValue(value, unit) !important

max-width-scope(value, unit = '')
  {props-sizing-max-width-property}: getWidthValue(value, unit) !important

max-height-scope(value, unit = '')
  {props-sizing-max-height-property}: getHeightValue(value, unit) !important
