for prop in (props-text-align-values)
  .{prefix-global}{props-text-align-class}-{prop}
    text-align-scope(prop)

for prop in (props-text-decoration-values)
  .{prefix-global}{props-text-decoration-class}-{prop}
    text-decoration-scope(prop)

for prop in (props-text-justify-values)
  .{prefix-global}{props-text-justify-class}-{prop}
    text-justify-scope(prop)

for prop in (props-text-overflow-values)
  .{prefix-global}{props-text-overflow-class}-{prop}
    text-overflow-scope(prop)

for prop in (props-text-transform-values)
  .{prefix-global}{props-text-transform-class}-{prop}
    text-transform-scope(prop)

for prop in (props-text-white-space-values)
  .{prefix-global}{props-text-white-space-class}-{prop}
    white-space-scope(prop)

for prop in (props-text-word-break-values)
  .{prefix-global}{props-text-word-break-class}-{prop}
    word-break-scope(prop)

for prop in (props-text-word-wrap-values)
  .{prefix-global}{props-text-word-wrap-class}-{prop}
    word-wrap-scope(prop)

