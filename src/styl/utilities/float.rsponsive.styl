float-responsive-scope(key)
  if (utilities-enabled && utilities-float-enabled && utilities-float-responsive)
    for prop in (utilities-float-values)
      .{prefix-global}{utilities-float-class}-{key}-{prop}
        float-scope(prop)
